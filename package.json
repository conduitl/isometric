{
  "name": "engine-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "docs:build": "pnpm --filter docs build",
    "test": "vitest run --project unit",
    "test:visual": "vitest run --project browser",
    "test:all": "vitest run",
    "test:watch": "vitest --project unit",
    "perf": "node scripts/perf/tilemap-budget.mjs",
    "perf:editor": "node scripts/perf/editor-paint-budget.mjs",
    "e2e": "pnpm --filter editor e2e",
    "lint": "eslint .",
    "typecheck": "pnpm -r run typecheck",
    "dev:demo": "pnpm --filter bedrock-demo dev",
    "dev:editor": "pnpm --filter editor dev",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "@vitest/browser": "^4.1.10",
    "@vitest/browser-playwright": "^4.1.10",
    "eslint": "catalog:",
    "playwright": "^1.62.1",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
